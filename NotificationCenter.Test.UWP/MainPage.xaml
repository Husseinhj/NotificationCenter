<Page
    x:Class="NotificationCenter.Test.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NotificationCenter.Test.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Border
            VerticalAlignment="Top"
            BorderThickness="0,1"
            BorderBrush="Gray"
            >
            <TextBlock
                Name="LblMessage"
                FontSize="25"
                Text="New message '{0}' was received."
                TextWrapping="WrapWholeWords"
                Margin="10"
            />
        </Border>
        <Button
            Name="BtnSubscribeActionOne"
            Click="BtnSubscribe_OnClick"
            Content="Subscribe one" />
        <Button
            Name="BtnSubscribeActionTwo"
            Click="BtnSubscribeActionTwo_OnClick"
            Margin="120,0,0,0"
            Content="Subscribe two" />
    </Grid>
</Page>
